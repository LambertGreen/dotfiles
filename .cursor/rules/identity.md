# Working Identity

See `~/.cursorrules` for full working identity and collaboration guidelines.

The complete identity file is deployed to `~/.cursorrules` via dotfiles stow and contains:
- Core principle: Text-based + programmable + AI-legible tools
- Epistemological loop methodology
- Weapon stack (Emacs, Org-mode, SQL, etc.)
- Working context and collaboration preferences

For the full identity context, refer to: `~/.cursorrules`
