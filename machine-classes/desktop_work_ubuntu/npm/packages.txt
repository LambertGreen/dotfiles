# NPM global packages for desktop work Ubuntu
# Principle: Keep minimal - editors manage their own dependencies
# Strategy: Start small, expand as needs are discovered

# TypeScript compiler for standalone work (org-mode blocks, one-offs, REPL)
typescript

# Salesforce CLI (provides sfdx executable) for GUS integration and mcp-server-gus
@salesforce/cli

# MCP server development tool

# Standalone formatter CLI for quick fixes
prettier
