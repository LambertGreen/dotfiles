# MSYS2 packages - POSIX gap filler (not primary shell)
# Only leaf applications - pacman handles dependencies

# Core POSIX utilities
git
tmux
vim
stow

# Development tools
python
python-pip

# Network tools
openssh
rsync
wget
curl

# Text processing
dos2unix

# MinGW packages (Windows native, architecture-specific)
# UCRT64 for x86_64 (recommended default), CLANGARM64 for ARM64
mingw-w64-ucrt-x86_64-gcc
